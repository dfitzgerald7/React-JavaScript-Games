import React from 'react';

const Twenty48Blocks = props => {
    
    for (let i=1; i<5; i++){
        for (let j=1; j<5; j++){

        }
    }



    return ( 
        <rect x={props.offSetX + 100} y={props.offSetY}
        width={props.width/4} height={props.width/4}
        style={{fill:'blue'}}
        /> );
}
 
export default Twenty48Blocks;